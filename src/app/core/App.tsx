import { BrowserRouter } from "react-router-dom";
import Routing from "./Routes";

const App = () => {
  return (
    <BrowserRouter>
      <Routing />
    </BrowserRouter>
    
  );
};

export default App;
